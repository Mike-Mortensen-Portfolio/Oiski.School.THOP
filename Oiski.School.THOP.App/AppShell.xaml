<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Oiski.School.THOP.App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:Oiski.School.THOP.App.Views"
    Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <Tab Title="Home"
         Icon="home.png">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate views:MainPage}"
                Route="MainPage" />
        </Tab>
        <Tab Title="Graph"
         Icon="graph.png">
            <ShellContent
                Title="Humidex Graph"
                ContentTemplate="{DataTemplate views:GraphPage}"
                Route="Graph" />
        </Tab>
        <Tab Title="Alarm"
         Icon="alarm.png">
            <ShellContent ContentTemplate="{DataTemplate views:GraphPage}" />
        </Tab>
    </TabBar>
</Shell>
